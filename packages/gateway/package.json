{
  "name": "@anima/gateway",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "predev": "bun run ../../scripts/generate-extension-web-contributions.ts && (lsof -ti:30086 | xargs kill -9 2>/dev/null || true)",
    "dev": "bun run src/start.ts",
    "prestart": "bun run ../../scripts/generate-extension-web-contributions.ts",
    "start": "bun run src/start.ts",
    "prebuild": "bun run ../../scripts/generate-extension-web-contributions.ts",
    "build": "bun build src/start.ts --outdir dist --target bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json"
  },
  "dependencies": {
    "@anima/shared": "workspace:*",
    "@anima/ui": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "react-markdown": "catalog:",
    "remark-gfm": "4.0.1",
    "zod": "catalog:",
    "zod-to-json-schema": "catalog:"
  },
  "devDependencies": {
    "@tailwindcss/typography": "catalog:",
    "bun-plugin-tailwind": "catalog:",
    "tailwindcss": "catalog:"
  }
}
